import React from 'react'

const Holiday = ({name, country, date}) => (
    <div>
        <div>{name}</div>
        <div>{country}</div>
        <div>{date}</div>
        <br />
    </div>
)

export default Holiday